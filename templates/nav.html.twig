
<nav class="nav-tabs navbar navbar-expand-lg navbar-light bg-light pb-0" style="background-color: #8da9b7">
    <div class="container-fluid">
        <a class="navbar-brand" style="color: #93c5fd" href="#">LeadStorage</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Переключатель навигации">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.get('_route') == 'app_leads' ? 'active' : '' }}" aria-current="page" href="{{ path('app_leads') }}">Лиды</a>
                </li>
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'app_admin' ? 'active' : '' }}" href="{{ path('app_admin') }}">Пользователи</a>
                    </li>
                {% endif %}
            </ul>
{#            <div class="d-flex ">#}
                <button class="btn btn-outline-primary btn-sm" id="copy"  data-clipboard-text="{{ url('app_lead_add') ? url('app_lead_add') ~ '?subid={subid}&sub_id_11='~ app.user.id ~'&from={source}&payout={revenue}&status={status}&country={country}' }}">
                    Ваша ссылка
                    <svg style="margin-bottom: 3px" id="svgCopy" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-clipboard" viewBox="0 0 16 16">
                        <path d="M4 1.5H3a2 2 0 0 0-2 2V14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V3.5a2 2 0 0 0-2-2h-1v1h1a1 1 0 0 1 1 1V14a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V3.5a1 1 0 0 1 1-1h1v-1z"/>
                        <path d="M9.5 1a.5.5 0 0 1 .5.5v1a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5v-1a.5.5 0 0 1 .5-.5h3zm-3-1A1.5 1.5 0 0 0 5 1.5v1A1.5 1.5 0 0 0 6.5 4h3A1.5 1.5 0 0 0 11 2.5v-1A1.5 1.5 0 0 0 9.5 0h-3z"/>
                    </svg>
                </button>
        </div>
    </div>
</nav>